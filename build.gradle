buildscript {
    ext {
        springBootVersion = '2.0.4.RELEASE'
    }
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:${springBootVersion}")
    }
}

ext {
    springCloudVersion = 'Finchley.SR1'
}

def lombokVersion = '1.18.2'
def modelMapperVersion = "2.1.1"
def swagger2Version = "2.9.2"

allprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    apply plugin: 'org.springframework.boot'
    apply plugin: 'io.spring.dependency-management'

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
    }

    dependencyManagement {
        imports {
            mavenBom "org.springframework.cloud:spring-cloud-dependencies:${springCloudVersion}"
        }
    }

    group = 'br.com.uol.test'
    version = '0.0.1-SNAPSHOT'
}

dependencies {
    compile project(":geoclima-application")
    compile project(":geoclima-api")
    compile project(":geoclima-service")
    compile project(":geoclima-ip-vigilante-client")
    compile project(":geoclima-meta-weather-client")
    compile project(":geoclima-persistence")
}

subprojects {
    dependencies {
        compile "org.modelmapper:modelmapper:${modelMapperVersion}"
        compileOnly "org.projectlombok:lombok:${lombokVersion}"
        compile "io.springfox:springfox-swagger2:${swagger2Version}"
        compile "io.springfox:springfox-swagger-ui:${swagger2Version}"
    }
}
